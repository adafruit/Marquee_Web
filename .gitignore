# --- macOS -------------------------------------------------------------------
.DS_Store
.AppleDouble
.LSOverride
._*

# --- Editors -----------------------------------------------------------------
.vscode/
.idea/
*.swp
*~

# --- Node --------------------------------------------------------------------
node_modules/
npm-debug.log*
yarn-error.log*
.npm/

# --- Secrets -----------------------------------------------------------------
# AIO_USER / AIO_KEY. Never commit a real one; the variables are documented
# in the README's Configuration section.
.env
.env.*

# --- Runtime state -----------------------------------------------------------
# The editor POSTs the whole canvas document to /canvas on every edit, so this
# file churns constantly and is per-bench, not source. A fresh clone has none:
# GET /canvas answers null and the first edit writes it.
data/canvas.json

# A6-A smoke-test fixture (a fake firmware manifest + image served from public/)
public/__fixture/

*.log
