{
  "name": "marquee-web",
  "version": "1.0.0",
  "description": "Adafruit IO Marquee — a static, browser-only e-ink canvas editor that renders panel bitmaps in pure JS",
  "type": "module",
  "scripts": {
    "start": "node scripts/serve.js",
    "test": "node --test \"test/**/*.test.js\" \"test/**/*.test.mjs\""
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/adafruit/Marquee_Web.git"
  }
}
